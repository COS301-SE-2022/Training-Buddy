import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class RepoService {

  //inject the service into a constructor:
    //private repo: RepoService
    //disable the lint for the error on the import

  constructor() { 
    //
  }

  //repo API calls to me moved to this service file

}
